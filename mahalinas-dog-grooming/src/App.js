import React from "react";
import MainContainer from "./components/MainContainer"; // Assuming MainContainer.js is in components directory
import EmailPopup from "./components/main/EmailModal";

const App = () => {
  return (
    <div className="app">
      <MainContainer />
    </div>
  );
};

export default App;
